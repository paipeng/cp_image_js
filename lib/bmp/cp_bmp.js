/**
 * @author Pai Peng
 *
 * gray color
 * 
 * 
 */

var writer = require('./lib/cp_bmp_write');
var reader = require('./lib/cp_bmp_read');

module.exports = {
    writer: writer,
    reader: reader
};
